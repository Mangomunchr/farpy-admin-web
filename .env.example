# Admin API auth (keep secret)
ADMIN_API_TOKEN=example_admin_token_32_chars

# Link signing (HMAC key you own; not Bunny’s)
LINK_SIGNING_SECRET=example-36-char-demo-secret-keep-safe

# Where files are served publicly (your Bunny Pullzone/CNAME)
BUNNY_PULL_BASE=https://cdn.farpy.com

# Public origin of this service (your CNAME on the host)
PUBLIC_BASE=http://localhost:4000
